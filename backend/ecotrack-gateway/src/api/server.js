/**
 * API Gateway Server Router
 * 
 * This module is deprecated. All routes are now defined in app.js
 * using the centralized proxy pattern.
 * 
 */

module.exports = (app) => {
  // All routes are now handled in app.js
  // This file is kept for backward compatibility
};
